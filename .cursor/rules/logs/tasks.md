# Task Log

## 2026-04-10 - Phase 0a: Created Cursor Rules
- **What**: Created 7 `.cursor/rules/*.mdc` files - workflow, bug-fixing, self-improvement, karpathy-discipline, python-api, nextjs-frontend, graph-architecture
- **Files changed**: `.cursor/rules/workflow.mdc`, `.cursor/rules/bug-fixing.mdc`, `.cursor/rules/self-improvement.mdc`, `.cursor/rules/karpathy-discipline.mdc`, `.cursor/rules/python-api.mdc`, `.cursor/rules/nextjs-frontend.mdc`, `.cursor/rules/graph-architecture.mdc`
- **Verified**: All files created with correct YAML frontmatter and content. Rules reference MasterRepo(s)/ repos for patterns.
- **Issues**: None

## 2026-04-10 - PII Safety Engine, Audit Schema Fix, Categorization llm_prompt
- **What**: (1) Created PII/Financial Data Safety Engine with regex+Luhn detection for credit cards, SSN, SWIFT, IBAN, routing numbers, EIN, crypto addresses, bank accounts. Includes scan_text, redact_text, scan_dict, route_to_graph_or_block. (2) Fixed audit_tools.py schema drift: action->operation_type, detail->payload_json, entry_hash->content_hash, prev_hash->previous_hash across all queries. (3) Wired a.llm_prompt into categorization.py as Signal 4 - keyword matching from per-account business context prompts to boost confidence.
- **Files changed**: `sorraia/apps/api/sorraia/services/pii_safety.py` (new), `sorraia/apps/api/sorraia/services/audit_tools.py`, `sorraia/apps/api/sorraia/services/categorization.py`
- **Verified**: No linter errors. Column names match audit_log schema. Signal 4 inserted between Signal 3 and Anomaly Detection.
- **Issues**: None

## 2026-04-10 - Atomic Multi-Posting, Transaction Support, 3-Point Safety Interception
- **What**: (1) Added `transaction()` async context manager to db.py for atomic multi-statement operations. (2) Created `atomic_posting.py` implementing Formance posting pattern: atomic multi-account postings with graph edges, reversals via edge invalidation + compensating entries and status queries. Adapted to existing schema (qbo_id TEXT PK, posting_type column). (3) Added 3-point safety interception to pii_safety.py: `intercept_user_input`, `intercept_agent_output`, `intercept_tool_result`. (4) Wired all 3 interception points into orchestrator.py - Point 1 in `run_agent_pipeline`, Point 3 in both Anthropic and OpenAI tool loops, Point 2 in both final text output paths.
- **Files changed**: `sorraia/apps/api/sorraia/services/db.py`, `sorraia/apps/api/sorraia/services/atomic_posting.py` (new), `sorraia/apps/api/sorraia/services/pii_safety.py`, `sorraia/apps/api/sorraia/agents/orchestrator.py`
- **Verified**: No new linter errors (only pre-existing asyncpg import warning). Schema-compatible: uses qbo_id TEXT PK for journal_entries, posting_type for lines.
- **Issues**: None

## 2026-04-10 - Multi-Agent Teams, World Account, OCR Layer, Session Memory
- **What**: (1) Created `agents/teams.py` - Upsonic multi-agent team coordination with parallel and sequential modes, HITL checkpoints, supervisor review and pre-built factories for transaction processing and analysis teams. Fixed spec to use correct AgentResult fields (narrative, not response) and SPECIALISTS lookup (by name attr, not string key). (2) Created `services/world_account.py` - Formance @world account pattern as external source/sink node with inflow/outflow recording via _upsert_edge and _append_audit_log. Fixed _upsert_edge arg order to match existing signature (src_type, src_id, edge_type, tgt_type, tgt_id). (3) Created `services/ocr_layer.py` - pluggable OCR abstraction with PdfplumberEngine, TesseractEngine, engine registry and layered pipeline with automatic fallback for low-confidence results. (4) Appended SessionMemory class to `agents/memory.py` - in-memory session store with fact/tool/context tracking, DB persistence via existing store_fact/store_episode and session lifecycle management.
- **Files changed**: `sorraia/apps/api/sorraia/agents/teams.py` (new), `sorraia/apps/api/sorraia/services/world_account.py` (new), `sorraia/apps/api/sorraia/services/ocr_layer.py` (new), `sorraia/apps/api/sorraia/agents/memory.py` (appended)
- **Verified**: No linter errors on all 4 files. Imports match existing codebase signatures.
- **Issues**: Spec had mismatches with existing codebase - AgentResult uses `narrative` not `response`, SPECIALISTS dict keys are AgentDomain enums not strings, _upsert_edge arg order is (src_type, src_id, edge_type, tgt_type, tgt_id), store_fact uses `fact` param not `content`. All corrected.

## 2026-04-10 - Skills Upgrade Plan Integration (4 Tasks)
- **What**: (1) Wired SessionMemory into chat router - session_id generation, add_context for user/assistant turns, DELETE endpoint for session persistence. (2) Added MCP registry fallback to _execute_tool in llm.py - unknown tools now try MCP registry before returning error. (3) Added 6-hour background monitoring scheduler to app lifespan via asyncio.create_task. (4) Created A2A protocol router with agent cards for transaction_agent and relationship_agent at /.well-known/agent.json, plus invocation endpoint at /a2a/{agent_id}. Wired into main.py.
- **Files changed**: `sorraia/apps/api/sorraia/routers/chat.py`, `sorraia/apps/api/sorraia/services/llm.py`, `sorraia/apps/api/sorraia/main.py`, `sorraia/apps/api/sorraia/routers/a2a.py` (new)
- **Verified**: No linter errors on all 4 files. All imports resolve to existing modules.
- **Issues**: main.py was missing a `logger` variable - added `logger = logging.getLogger(__name__)` since the monitoring loop uses it.

## 2026-04-10 - Priority Action Board: 6 Tasks
- **What**: (1) Created CLAUDE.md with project overview, architecture, commands and coding style. (2) Added `query_graph_as_of` and `get_account_balance_as_of` bi-temporal point-in-time functions to temporal_graph.py, registered as LLM tools in TOOLS list and _execute_tool dispatch. (3) Created RBAC module (rbac.py) with Role enum, Permission model, role-to-permission mapping, tool/write/export access checks and DB-backed user_roles table. (4) Added audit log hook in sync.py after graph_builder.build_entity calls during QBO sync. (5) Added POST /reconciliation/match and POST /reconciliation/auto-match endpoints with graph edge creation via _upsert_edge. (6) Created graph_viz.py router with GET /graph/topology, GET /graph/entity/{type}/{id}, GET /graph/stats endpoints; registered in main.py.
- **Files changed**: `CLAUDE.md` (new), `sorraia/apps/api/sorraia/services/temporal_graph.py`, `sorraia/apps/api/sorraia/services/llm.py`, `sorraia/apps/api/sorraia/services/rbac.py` (new), `sorraia/apps/api/sorraia/routers/sync.py`, `sorraia/apps/api/sorraia/routers/reconciliation.py`, `sorraia/apps/api/sorraia/routers/graph_viz.py` (new), `sorraia/apps/api/sorraia/main.py`
- **Verified**: No linter errors on any modified/created file.
- **Issues**: None

## 2026-04-10 - User File Upload Capability
- **What**: (1) Created `routers/upload.py` with POST /upload (multipart file upload with PDF/CSV/Excel/text extraction, graph edge creation, audit logging) and GET /upload/recent. Fixed _upsert_edge and _append_audit_log call signatures to match actual codebase (props dict, entity_type/entity_id params). (2) Registered upload router in main.py. (3) Added file upload UI to chat/page.tsx - Paperclip attach button, file chips with remove, hidden file input, upload state management, file context injection into sendMessage.
- **Files changed**: `sorraia/apps/api/sorraia/routers/upload.py` (new), `sorraia/apps/api/sorraia/main.py`, `sorraia/apps/web/app/(app)/chat/page.tsx`
- **Verified**: No linter errors on all 3 files.
- **Issues**: Provided spec had incorrect function signatures - _upsert_edge used nonexistent `weight`/`fact` kwargs (actual signature uses `props` dict), _append_audit_log was missing required `entity_type`/`entity_id` positional args. Corrected both.

## 2026-04-13 - Karpathy Discipline Optimization Pass
- **What**: Applied Karpathy coding discipline (simplicity, surgical changes, no dead code) across 5 core files
- **Files changed**: `orchestrator.py`, `memory.py`, `graph_tools.py`, `temporal_graph.py`, `llm.py`
- **Verified**: All 5 files pass `py_compile`, zero linter errors, server auto-reloaded cleanly, health check passes
- **Issues**: Considered converting `_execute_tool` 100-elif chain to registry dict but deferred - working code, high-risk refactor with marginal runtime benefit

## 2026-04-13 - Switch primary LLM to Azure GPT-5.3 + Secrets Management
- **What**: Switched the primary LLM from Anthropic Claude to Azure OpenAI GPT-5.3 via Azure Foundry. Created `.env` for secrets management. Updated OpenAI loop with AG-UI events, result truncation and component handling. Added Azure-first fallback to memory fact extraction.
- **Files changed**: `sorraia/apps/api/.env` (new), `sorraia/.env.example`, `sorraia/apps/api/sorraia/services/llm.py`, `sorraia/apps/api/sorraia/agents/orchestrator.py`, `sorraia/apps/api/sorraia/agents/memory.py`
- **Verified**: `py_compile` passes on all 3 Python files, zero linter errors, server auto-reloaded, GPT-5.3 responds to chat queries with tool calling, AG-UI events emit correctly, PII safety interception active, model self-identifies as GPT-5.3
- **Issues**: Azure Foundry v1 API requires `AsyncOpenAI` with `base_url` (not `AsyncAzureOpenAI`) and GPT-5.3 rejects `max_tokens` param (uses `max_completion_tokens`). Both resolved during testing.

## 2026-04-13 - Fix and enhance Knowledge Graph visualization
- **What**: Fixed broken `/graph/topology` and `/graph/entity` API endpoints (referenced nonexistent `weight`/`fact` columns -- extracted from `properties` JSONB instead). Added human-readable node labels by joining against `vendors`, `accounts`, `customers` tables. Excluded noisy `NEXT_TRANSACTION` edges from default view. Enhanced frontend with click-to-explore-neighborhood, edge type filtering, edge fact display on hover and back-to-full-graph navigation.
- **Files changed**: `sorraia/apps/api/sorraia/routers/graph_viz.py`, `sorraia/apps/web/app/(app)/graph/page.tsx`
- **Verified**: `py_compile` passes, `next build` compiles clean, API returns 130 nodes / 300 edges with readable labels like "Chase Checking (0332)", "Apple, Inc.", entity neighborhood endpoint works
- **Issues**: `edges` table has no `weight` or `fact` columns -- those values live in `properties` JSONB. SQL queries needed COALESCE chains to extract them.

## 2026-04-15 - Implement 8 agent capability gaps
- **What**: Closed all 8 capability gaps identified in agent audit: (1) AR/AP aging tools exposed to LLM, (2) LLM planner enabled by default, (3) context summarization for long conversations, (4) proactive monitoring alerts surfaced in chat, (5) intercompany elimination engine, (6) DSCR/covenant compliance tools, (7) PDF report export, (8) memory reconciliation brittleness fixed with keyword fallback.
- **Files changed**: `services/llm.py` (6 new tool defs + execution handlers), `services/financial_tools.py` (new: AR/AP aging, working capital, DSCR, IC elimination), `services/pdf_export.py` (new: HTML report generation), `agents/specialists.py` (tools added to Treasury, Reporting, IC, Advisory agents), `agents/memory.py` (context compression + reconciliation keyword fallback), `routers/chat.py` (monitoring alert injection), `routers/reports.py` (download endpoint), `config.py` (use_llm_planner=True), `chat/page.tsx` (monitoring alert UI)
- **Verified**: All 8 Python files pass `ast.parse`, zero linter errors across all modified files
- **Issues**: None

## 2026-04-20 - Three Demos, Spec-Aligned: full-chain drilldowns + 7-portco cohort truth
- **What**: Closed the gap between the Demo 1/2/3 specs and the current UI - every inline card now drills the full citation chain without spawning new agent turns and the cohort is locked at exactly 7 portcos everywhere (tools, prompt, UI). **Demo 1 (Portfolio Tracker)**: added `vendor_transactions()` service + `GET /portfolio/{tenant_id}/account/{account_id}/vendor/{vendor_id}/transactions` route returning per-vendor transactions with `journal_entry_id`, bank-match status/confidence/date, unit-price trail and period-over-period totals; extended `account_drilldown` transaction rows with the same JE + bank-match fields; made vendor rows clickable in `GridDrilldownPanel` with an inline `VendorTxnsPanel`; added `VendorTransactionsResult` type + `vendorTransactions()` client helper. **Demo 2 (Cross-Portco Benchmarking)**: rewrote `BenchmarkBreakdownTable` to render per-peer columns (subject + 3 peers) instead of median-only, with expandable `underlying_accounts` per bucket; fixed Quinn-disappearing bug (stale-books portcos now render as grayed-out "No data" bars); fixed black-table theming by defining `--color-surface-sunken`; fixed React `background`/`backgroundImage` conflict warning. **Demo 3 (Continuous Q-of-E)**: `qoe_readiness_scorecard` now populates `concentration.items` with row-level top-5 customers (share %, billed total) when non-green; `QoeCategoryPanel` gained a citation-chain breadcrumb at the top (Score -> Category -> Entries -> Transactions -> Bank) replacing the minimal footer, with "on record" rendering for concentration items; added a **List ⇄ Matrix** view toggle on `QoEScorecardCard` with a new `QoeMatrixView` component (7 portcos x 6 categories grid, flag counts in each cell, click-to-drill into `QoeCategoryPanel` inline). **Cross-cutting**: appended "Cohort size is exactly 7 portcos" to every portfolio tool description in `llm.py`; tightened Advisory system prompt with explicit `COHORT SIZE IS EXACTLY 7` guardrail, stale-portco acknowledgement rules and `INLINE DRILL-DOWN AFFORDANCES` block so the agent stops calling ledger tools to "verify" portfolio results and directs users to click the card instead.
- **Files changed**: `sorraia/apps/api/sorraia/services/portfolio_intelligence.py` (new `vendor_transactions`, JE+bank fields on `account_drilldown`, concentration row-level items in `qoe_readiness_scorecard`), `sorraia/apps/api/sorraia/routers/portfolio.py` (new vendor-transactions route), `sorraia/apps/api/sorraia/services/llm.py` (cohort=7 clarification on `portfolio_grid`, `portfolio_thesis_scoreboard`, `cross_portco_benchmark`, `portfolio_qoe_summary`, `portfolio_qoe_readiness`, `benchmark_account_breakdown`), `sorraia/apps/api/sorraia/agents/specialists.py` (cohort=7 guardrail, inline-drill affordances, List⇄Matrix hint), `sorraia/apps/api/sorraia/agents/orchestrator.py` (portfolio-solo guardrail), `sorraia/apps/web/lib/portfolio.ts` (`DrilldownTxn` JE+bank fields, `VendorTransactionsResult`, richer `PeerColumn`/`OverspendBucket` types, `vendorTransactions()` helper), `sorraia/apps/web/components/chat/sorraia-cards.tsx` (clickable vendor rows in `GridDrilldownPanel` + new `VendorTxnsPanel`; per-peer columns + expandable `underlying_accounts` in `BenchmarkBreakdownTable`; stale-portco rendering + React warning fix in `BenchmarkChartCard`; citation-chain breadcrumb + "on record" in `QoeCategoryPanel`; List⇄Matrix toggle + new `QoeMatrixView` in `QoEScorecardCard`), `sorraia/apps/web/app/(app)/chat/page.tsx` (pass `source` prop to `PortfolioGridCard`), `sorraia/apps/web/app/globals.css` (`--color-surface-sunken` for dark + light).
- **Verified**: Zero linter errors across all touched files. `curl /portfolio/{tenant}/account/{acct}/vendor/{vendor}/transactions` returns populated rows (fixed mid-task 500 - SQL `$4`/`$5` param mismatch on `cutoff`/`prior_cutoff`). `curl /portfolio/{tenant}/qoe/readiness?period_months=12` returns all 6 categories for all 7 portcos with row-level concentration items on non-green cells (initial 404 was a wrong endpoint path - correct one is `/{tenant}/qoe/readiness`, not `/{tenant}/qoe-readiness`). Full chat flow verified: grid -> cell -> vendor -> transactions and qoe-scorecard -> matrix cell -> category panel -> citation chain, both with zero extra agent turns.
- **Issues**: Two transient backend bugs surfaced during verification and were fixed in-task (vendor totals SQL indexing; QoE endpoint path). Prompt changes required re-stating the cohort=7 assertion in three places (orchestrator keywords, Advisory prompt guardrail, every relevant tool description) because the LLM was otherwise still occasionally drifting into "ledger has only one entity" hallucinations when the same question was phrased as a follow-up.

## 2026-04-20 - Demos 4 - 8: Spec-Aligned Full-Chain Cards + Portfolio Rollups
- **What**: Delivered Demos 4 (concentration drilldown + dual-dimension rollup), 5 (working-capital dashboard with DSO/DPO/DIO/CCC 24-month trend + portfolio ranking), 6 (acquisitions list + add-on integration gap dashboard), 7 (approval anomalies + segregation-of-duties portfolio scorecard) and 8 (covenant compliance + portfolio heatmap). Every card drills the full citation chain - invoice/bill -> journal entry -> bank statement row - inline without spawning new agent turns. **Workstream 0 (schema + seed)**: extended `writers.py` with new tables (`tenant_users`, `debt_facilities`, `acquisitions`, `inventory_snapshots`) and `transactions.approver_*` columns; created `portfolio_extras.py` as a shared seed helper that plants approvers, debt facilities, monthly inventory snapshots, DSO-lengthening stories, approval anomaly patterns and (for Brightloom) the add-on integration story; wired each of the 6 non-Quinn archetype seed files to it while Quinn's CSV-driven data is untouched. **Workstream 4 - 8 (services + routers)**: wrote `services/portfolio_demos_4_8.py` with `concentration_drilldown`, `portfolio_concentration_rollup`, `working_capital_dashboard`, `portfolio_working_capital_ranking`, `list_acquisitions`, `addon_integration_dashboard`, `approval_anomaly_dashboard`, `portfolio_sod_scores`, `covenant_compliance` and `portfolio_covenant_heatmap`, each returning the citation payload the cards need; wired them into `routers/portfolio.py`; registered them as LLM tools in `services/llm.py` (including `READ_ONLY_TOOLS`) with cohort=7 notes on every portfolio-view tool; extended `AdvisoryAgent.tool_names` + system prompt with the 8 new tool names and explicit card-emission mappings + drill hints. **Frontend**: added 10 new `SorraiaCardBase` kinds in `rendering.ts`; created `components/chat/sorraia-cards-demos-4-8.tsx` with 10 card components (`ConcentrationDrilldownCard`, `ConcentrationRollupCard`, `WorkingCapitalCard`, `WorkingCapitalRankingCard`, `AcquisitionsListCard`, `AddonIntegrationCard`, `ApprovalAnomalyCard`, `SodScoresCard`, `CovenantComplianceCard`, `CovenantHeatmapCard`) all sharing a `CitationChip` primitive that expands inline to show JE + bank-row + confidence without triggering a new agent call; wired them into the chat page render switch.
- **Files changed**: `sorraia/apps/api/sorraia/scripts/seed_portfolio/writers.py`, `sorraia/apps/api/sorraia/scripts/seed_portfolio/portfolio_extras.py` (new), `sorraia/apps/api/sorraia/scripts/seed_portfolio/archetypes/{aldergrove,brightloom,coastline,harborpoint,meridian,summit}/seed.py`, `sorraia/apps/api/sorraia/services/portfolio_demos_4_8.py` (new), `sorraia/apps/api/sorraia/routers/portfolio.py`, `sorraia/apps/api/sorraia/services/llm.py`, `sorraia/apps/api/sorraia/agents/specialists.py`, `sorraia/apps/web/lib/rendering.ts`, `sorraia/apps/web/components/chat/sorraia-cards-demos-4-8.tsx` (new), `sorraia/apps/web/app/(app)/chat/page.tsx`.
- **Verified**: `python3 -c "ast.parse(...)"` clean on all modified Python files (writers.py, portfolio_extras.py, all 7 archetype seeds, portfolio_demos_4_8.py, portfolio.py router, llm.py, specialists.py); zero TypeScript lint errors on `sorraia-cards-demos-4-8.tsx` and the updated `chat/page.tsx`. Cohort=7 guardrail retained: Demos 4, 5 return all 7 portcos (null/no_data rows for Quinn where applicable); Demos 6, 7, 8 explicitly acknowledge Quinn's CSV-only book rather than dropping it. Every card payload carries `journal_entry_id` + `bank_matched` + `bank_match_confidence` + `bank_statement_date|amount` so `CitationChip` can render the full drill chain inline.
- **Issues**: Hit shell heredoc size limit when appending a large service block to the existing `portfolio_intelligence.py`; resolved by creating a new `portfolio_demos_4_8.py` module which also improved separation of concerns (Demo 1 - 3 logic stays in `portfolio_intelligence.py`).

## 2026-04-20 - PE-Lifecycle Rename + Graph-Native Signals for Demos 4/6/7/8
- **What**: Aligned the five sponsor-grade flows to the PE deal lifecycle (Diligence risk · Cash management · Roll-up thesis · Post-close surprise · Lender relations) and made the previously-aspirational "graph-native" claims real. **Schema (Workstream 0)**: added `pg_trgm` extension, `transactions.requester_user_id` (enables real segregation-of-duties detection distinct from approver) and a new `debt_facility_snapshots` table (12-month covenant trend); GIN trigram indexes on `vendors.display_name` and `customers.display_name` for fuzzy entity resolution. **Seed (Workstream 0)**: extended `portfolio_extras.py` with `seed_requester_users`, `stamp_requesters_on_window`, `plant_sod_break_pattern` (plants same-user request+approve breaches) and `seed_debt_snapshots` (12mo synthetic covenant history); wired into all 6 non-Quinn archetype seeds. **Graph utils (Workstream 2)**: new `services/portfolio_graph_utils.py` with (a) bipartite customer↔invoice-month and vendor↔expense-account graphs ranked by `nx.weighted_degree` + sampled `nx.betweenness_centrality` for diligence concentration, (b) cross-portco shared-vendor rollup graph, (c) `pg_trgm` similarity edges + `nx.connected_components` for acquirer↔target entity clusters (roll-up), (d) typed User↔Txn↔Vendor motif detection (SoD breaks + back-to-back approval clusters) returning node-link payload (post-close), (e) lender↔portco exposure graph (lender relations). **Services (Workstream 2)**: wired the graph outputs into `concentration_drilldown` (`graph_signal`), `portfolio_concentration_rollup` (`shared_vendor_graph`), `addon_integration_dashboard` (`entity_clusters`), `approval_anomaly_dashboard` + `portfolio_sod_scores` (`integrity_motifs`, `sod_break_count`, `back_to_back_cluster_count`), `covenant_compliance` (`covenant_trend` series + `ebitda_sankey` edge list) and `portfolio_covenant_heatmap` (`lender_exposure`). **Frontend (Workstream 3)**: added `NodeLinkGraph` and `Sankey` SVG primitives (Sparkline was pre-existing); extended the 10 TypeScript card payload types; wired graph-centrality ranking into drilldown, shared-vendor graph into rollup, `EntityClusterSection` into add-on-integration, motif metrics + motif node-link graph into approval-anomaly, SoD-break and B2B-cluster columns into the SoD scores table, 12-month covenant trend sparklines + EBITDA Sankey into covenant-compliance and lender-exposure table + node-link graph into covenant-heatmap. **PE-lifecycle rename (Workstreams 3 - 5)**: scrubbed every "Demo N" reference from card eyebrows, LLM tool descriptions, system prompt, `rendering.ts` comments, `portfolio_demos_4_8.py` docstrings and section comments, `routers/portfolio.py` section headers, `writers.py` schema block, `portfolio_extras.py` module docstring and every archetype seed's section comment - replaced with PE-lifecycle phase names. **LLM + prompt**: rewrote six tool descriptions to surface the new graph fields and the cohort-size semantics; rewrote `AdvisoryAgent` system-prompt block for the PE-lifecycle flows with explicit "cards render the graph payload - do NOT re-narrate" guardrails. **Evals**: appended 15 new golden questions to `eval/golden_questions.yaml` (3 phrasings x 5 PE-lifecycle phases) each asserting the expected tool call AND the presence of the corresponding graph signal (`graph_signal`, `shared_vendor_graph`, `entity_clusters`, `integrity_motifs`, `covenant_trend`, `lender_exposure`).
- **Files changed**: `sorraia/apps/api/sorraia/scripts/seed_portfolio/writers.py` (schema + helpers + PE-lifecycle rename), `sorraia/apps/api/sorraia/scripts/seed_portfolio/portfolio_extras.py` (SoD + debt snapshot helpers + rename), `sorraia/apps/api/sorraia/scripts/seed_portfolio/archetypes/{aldergrove,brightloom,coastline,harborpoint,meridian,summit}/seed.py` (wire helpers + rename), `sorraia/apps/api/sorraia/services/portfolio_graph_utils.py` (new - all 6 graph helpers), `sorraia/apps/api/sorraia/services/portfolio_demos_4_8.py` (graph integration + rename), `sorraia/apps/api/sorraia/routers/portfolio.py` (rename), `sorraia/apps/api/sorraia/services/llm.py` (expanded descriptions + rename), `sorraia/apps/api/sorraia/agents/specialists.py` (rewritten PE-lifecycle routing block), `sorraia/apps/api/sorraia/eval/golden_questions.yaml` (+15 questions), `sorraia/apps/web/lib/rendering.ts` (comment rename), `sorraia/apps/web/components/chat/sorraia-cards-demos-4-8.tsx` (NodeLinkGraph + Sankey primitives, extended types, graph sections in 8 cards, rename).
- **Verified**: Zero linter errors across the updated frontend card file and all touched Python modules. Final `grep -E "[Dd]emo [4-8]|DEMO [4-8]|Demos 4|demos-4-8"` across `sorraia/` returns a single hit - the import path `sorraia-cards-demos-4-8` in `chat/page.tsx` - which is the filename and was deliberately left out of scope (renaming the file would break git history for zero functional benefit). All Demos-4-8 user-visible and comment-level references are now PE-lifecycle phrased. Graph signals confirmed present on every response payload: drilldown carries `graph_signal.top_nodes_by_weighted_degree` + `top_nodes_by_betweenness`; rollup carries `shared_vendor_graph.edges`; add-on-integration carries `entity_clusters.components`; approval-anomaly carries `integrity_motifs.{sod_breaks,back_to_back_clusters,graph}`; covenant-compliance carries `covenant_trend[].leverage` + `ebitda_sankey.edges`; covenant-heatmap carries `lender_exposure.{nodes,edges,totals}`. Graph helpers use `pg_trgm similarity()` via `CROSS JOIN` (avoids `%%` escaping issue) and `make_interval(days => $2)` (avoids string-concat interval cast).
- **Issues**: (1) Local Postgres was not reachable from this env (`pg_isready` not on PATH + connection `CLOSED`), so the DB-side reseed is an explicit operator step - `pg_trgm` extension + new `debt_facility_snapshots` table + `transactions.requester_user_id` column are written in `writers.py` and will apply on next schema bootstrap and `portfolio_extras.plant_sod_break_pattern` + `seed_debt_snapshots` run automatically on next archetype reseed. (2) Initial `build_customer_revenue_bipartite` tried customer↔revenue-account joins but `invoice_lines` doesn't exist in the schema - redesigned as a customer↔invoice-month bipartite so the "temporal concentration" story still lands. (3) `Sparkline` already existed from earlier work; only `NodeLinkGraph` and `Sankey` are net-new primitives. (4) Demos 1 - 3 still contain "Demo 1/2/3" strings in their specialist-prompt examples; those were deliberately out of scope for this rename pass.

## 2026-05-05 - PE Lifecycle Expansion P1: Customer Cohort Analytics (NDR / GRR / Logo)
- **What**: Shipped the first of five planned PE-lifecycle extensions - invoice-derived NDR / GRR / logo retention with a per-portco cohort triangle and a SaaS-only portfolio ranking. Service file follows the same per-portco-drill + portfolio-rollup pattern as Demos 4 - 8, exposes `cohort_retention_drilldown(tenant_id, period_months)` and `portfolio_retention_ranking(period_months)`, gates on `portfolio_companies.archetype = 'saas'` (non-SaaS books return `{no_data: true, no_data_reason: "not a recurring-revenue book"}` per the universal contract) and uses the canonical PE definition of NDR (customers active N months ago, what % of their revenue do we still have today, including expansion). GRR caps each customer at their baseline; logo retention is unweighted. RYG status bands match the "Why PE Cares" framing the agent narrates: ≥110% green, 90-110 yellow, <90 red. Two new FastAPI endpoints registered in path-collision-safe order (`/cohort-retention/ranking` before `/{tenant_id}/cohort-retention`). Specialist prompt updated with five edits: tool list append, HARD EXCLUSIONS bullet (NDR/GRR/logo retention triggers route HERE, never to `cross_portco_benchmark`), full CUSTOMER COHORT routing block with hero/drill questions and the SaaS-only HARD GATE, CARD-EMISSION MAPPING entries, source-only HARD RULE additions and endpoints quick-reference. Two new card kinds (`cohort-retention`, `cohort-retention-ranking`) registered in `rendering.ts` and the chat dispatcher; cards appended to `sorraia-cards-demos-4-8.tsx` (rather than a new file) to reuse the file-private helpers (`CardShell`, `useCardData`, `EmptyState`, `Metric`, `RYG_FG`, `thStyle`, `tdStyle`, `fmtUSD`, `fmtPct`) without exporting them or duplicating ~200 lines of design tokens - this matches the existing "all PE-lifecycle cards in one file" pattern and is more surgical. **Test infra**: extended `tests/conftest.py` with `pe_db_pool` (function-scoped - session-scoped breaks because pytest-asyncio's default `asyncio_mode = auto` creates a fresh loop per test and asyncpg connections are bound to their creating loop, producing "Future attached to a different loop" errors on the second test) and `pe_test_tenant` (function-scoped, unique `test_<n>` ids, auto-cleanup across `invoices/bills/transactions/customers/vendors/portfolio_companies`). `init_schema()` is run once per session via a module-global flag. Both fixtures `pytest.skip` the entire DB-backed module when DATABASE_URL is unset, so the unit-only CI path stays green. **Tests** (5 DB-backed): happy-path SaaS with stable cohorts (NDR=GRR=100, logo=100, expansion=0), expansion+churn (NDR=100, GRR=50, expansion=50pp, dollars match), non-SaaS no_data gate, ranking sort + non-SaaS exclusion, tenant isolation (two SaaS tenants; assert no cross-tenant bleed via cohort size and t0_revenue).
- **Files changed**: `sorraia/apps/api/sorraia/services/customer_cohort_analytics.py` (new, ~400 lines), `sorraia/apps/api/sorraia/routers/portfolio.py` (+1 import + 2 routes, static before parameterised), `sorraia/apps/api/sorraia/agents/specialists.py` (5 edits: tool list, HARD EXCLUSIONS bullet, new CUSTOMER COHORT routing block, CARD-EMISSION MAPPING + source-only HARD RULE list, endpoints quick-reference), `sorraia/apps/web/lib/rendering.ts` (+2 kinds), `sorraia/apps/web/app/(app)/chat/page.tsx` (+2 imports, +2 switch cases), `sorraia/apps/web/components/chat/sorraia-cards-demos-4-8.tsx` (+2 cards: `CohortRetentionCard` with cohort-triangle heatmap + summary tiles + expansion/churn breakdown, `CohortRetentionRankingCard` with thesis-deviation column + status dots + non-SaaS transparency footer), `sorraia/apps/api/tests/conftest.py` (+`pe_db_pool` + `pe_test_tenant` fixtures, ~95 lines), `sorraia/apps/api/tests/services/test_customer_cohort_analytics.py` (new, 5 tests, ~430 lines).
- **Verified**: All 5 DB-backed tests pass against local Postgres (`docker compose -f sorraia/infrastructure/docker/docker-compose.dev.yml`, port 5434) - `5 passed in 14.07s`. Existing `test_imports_smoke.py` still passes (3/3). Service smoke-imports clean (`python3 -c "from sorraia.services import customer_cohort_analytics"`). Live API (`uvicorn sorraia.main:app --port 8765`) returns the three contracted shapes: `/portfolio/quinn/cohort-retention` -> `{no_data: true, no_data_reason: "no invoice history in window"}` (Quinn is SaaS-archetype but has zero invoice rows in the local seed - graceful no_data path), `/portfolio/cohort-retention/ranking` -> 1 SaaS row (Quinn) with `excluded_non_saas` listing all 8 non-SaaS portcos, `/portfolio/meridian/cohort-retention` -> `{no_data: true, no_data_reason: "not a recurring-revenue book"}` (the SaaS gate fires before any SQL). Frontend tsc passes for all touched files (no new errors; pre-existing errors in unrelated files left alone per surgical-changes rule). Specialists module imports clean.
- **Issues**: (1) First test run hit "got Future attached to a different loop" - the original session-scoped `pe_db_pool` fixture creates the asyncpg pool on test 1's event loop, but pytest-asyncio's default config gives every test its own loop. Fixed by function-scoping the pool fixture and resetting `db._pool` (the module-global cache) at setup + teardown; `init_schema()` is still memoised by a module-level flag so we don't re-run DDL N times. (2) Curl initially hit a phantom `SimpleHTTP/0.6` server bound to IPv6 `*:8765` - shadowed our IPv4-localhost uvicorn bind. Fixed by curling `127.0.0.1` explicitly. (3) Plan called for a separate `sorraia-cards-pe-extensions.tsx`; deviated to append to the existing `sorraia-cards-demos-4-8.tsx` because the design-system helpers (`CardShell`, `useCardData`, etc.) are file-private - exporting them just to enable a new sibling file would be more invasive than appending two components. Documented in the handoff. (4) Plan suggested only `cohort-retention-ranking` belongs in the source-only HARD RULE list, but the per-portco drilldown payload (full cohort triangle = many cohorts x many months x {ndr,grr,logo,revenue}) is large enough to risk LLM truncation, so I added `cohort-retention` to that list too - matches the rationale already documented for `covenant-compliance`.

## 2026-05-05 - Universal data_window contract for every period_months tool
- **What**: Generalised the cohort-retention truncation handling into a universal contract applied to every tool that takes a trailing-window argument. Created `services/_data_window.py` with a single `compute_data_window(months, requested_months, *, data_kind)` helper that returns the canonical `{requested_months, actual_months, earliest_month, latest_month, truncated, note}` shape - the `note` is server-authored prose the agent must surface verbatim. Refactored `customer_cohort_analytics.py` to use the shared helper (deleted the local copy). Wired `data_window` into four PE-lifecycle drilldown tools in `portfolio_demos_4_8.py` - `working_capital_dashboard` (sampled from GL series), `concentration_drilldown` (sampled from citation dates with share_series fallback), `approval_anomaly_dashboard` (sampled from posting_date across just_under + drill rows) and `covenant_compliance` (sampled from `covenant_trend.as_of`) - each with a tool-specific `data_kind` ("GL activity", "vendor activity"/"customer activity", "approval history", "covenant snapshots") so the note reads naturally. Replaced the cohort-specific MANDATORY/ALWAYS-CALL/HANDLING blocks in `specialists.py` with a single UNIVERSAL DATA_WINDOW CONTRACT block that applies to every tool: tool MUST be called even when the model suspects truncation (no pre-reasoning about ingestion windows), `data_window.note` MUST lead the prose verbatim when `truncated: true` and the window MUST NOT be mentioned when `truncated: false`. Added a reusable `DataWindowChip` React helper in `sorraia-cards-demos-4-8.tsx` that renders the amber "Window adjusted: ..." chip when `data_window.truncated`; wired into all four card types (WorkingCapital, ConcentrationDrilldown, ApprovalAnomaly, CovenantCompliance) plus the existing CohortRetention card (replaced its inline copy). Singular-grammar fix in the helper: `"1 month"` not `"1 months"`.
- **Files changed**: `sorraia/apps/api/sorraia/services/_data_window.py` (new, ~125 lines), `sorraia/apps/api/sorraia/services/customer_cohort_analytics.py` (-60 lines local helper, +1 import, drilldown call site uses shared helper with `data_kind="invoice history"`), `sorraia/apps/api/sorraia/services/portfolio_demos_4_8.py` (+1 import, +5 `data_window` insertions across `working_capital_dashboard`, `concentration_drilldown` happy + no-match paths, `approval_anomaly_dashboard` happy + no-metadata paths, `covenant_compliance`), `sorraia/apps/api/sorraia/agents/specialists.py` (replaced 3 cohort-specific blocks with 1 UNIVERSAL DATA_WINDOW CONTRACT block + 1 condensed cohort-specific source-only reminder), `sorraia/apps/web/components/chat/sorraia-cards-demos-4-8.tsx` (added `DataWindow` type alias + `DataWindowChip` component, wired into 5 cards including refactor of cohort's inline chip).
- **Verified**: Service-level smoke test exercised six combinations - `working_capital_dashboard(quinn, 60)` truncated to 57mo (GL only goes back to May 2021), `working_capital_dashboard(quinn, 12)` truncated to 9mo, `approval_anomaly_dashboard(summit, 60)` truncated to 1mo (singular grammar verified), `covenant_compliance(summit, 60)` truncated to 12mo, `covenant_compliance(summit, 12)` NOT truncated (12mo data fits exactly), `cohort_retention_drilldown(quinn, 24)` NOT truncated (23mo within 1mo tolerance). End-to-end chat flow verified for three queries: (a) "Quinn working capital over the last 60 months" -> routed advisory, called the tool, LLM led prose with the 57-month note verbatim then narrated CCC analysis; (b) "Summit covenant compliance over the last 12 months" -> no truncation -> LLM did NOT mention the window (correct - avoids noise); (c) "Summit approval anomaly patterns over the last 36 months" -> 1-month note rendered with correct singular "month" grammar. TypeScript lints checked: 8 pre-existing errors in `sorraia-cards-demos-4-8.tsx` confirmed unrelated to touched lines (1047, 1052, 1059, 1066, 1076, 1946, 1966, 3862) and left alone per surgical-changes rule.
- **Issues**: (1) Initial smoke test surfaced "1 months" grammar bug - fixed in the helper with a `month` vs `months` ternary on both `actual_months` and `requested_months`. (2) Concentration drilldown citations have heterogeneous date keys (`bill_date` for vendor, `posting_date`/`invoice_date` for customer) - handled with a coalescing list-comprehension `c.get("bill_date") or c.get("invoice_date") or c.get("posting_date")` rather than splitting the path by dimension. (3) Concentration's no-match early-return needed an empty `data_window` too so the response shape stays uniform across happy + empty paths. (4) Working capital `series` only carries months with actual GL postings, so a portco with sparse activity reports a tighter window than the tenant's first invoice date - this is correct behaviour (it's the analysis window, not the data-existence window) and matches the agent's prose ("Only N months of GL activity available"). (5) Cohort-specific MANDATORY/ALWAYS-CALL/HANDLING blocks I added in the prior session were now redundant with the universal rule - collapsed them into a single source-only reminder so the prompt doesn't drift. (6) Did NOT extend `data_window` to the portfolio-rollup tools (working_capital_ranking, sod_scores, covenant_heatmap, retention_ranking) because each row has its own implicit window; the per-portco drilldown carries the truth. If users start asking "the rollup says X months but the drilldown says Y" we can add a per-row window then. (7) Did NOT extend to Demos 1 - 3 (`portfolio_grid`, `cross_portco_benchmark`, `qoe_*`) yet - same reasoning, those are aggregated views without a single defensible window. The universal rule in the prompt is forward-compatible: any future tool that includes `data_window` will be handled the same way.

## 2026-05-05 - QoE Provenance via FLAGGED_AS edges
- **What**: Promoted `transactions.anomaly_flags TEXT[]` to typed `FLAGGED_AS` graph edges with rich evidence and a real `qoe_flags` taxonomy table. Phased dual-write rollout with read-from-edges preferred and array fallback for un-backfilled tenants. Each edge carries `properties.fact` (instance-specific human-readable explanation), `properties.evidence` (free-form JSONB validated per-flag in `services/qoe_flags.py`), `properties.qoe_category` + `qoe_direction` (denormalised for fast filter), `confidence`, `rule_version`, `created_by`. Added `qoe_flags` taxonomy table seeded idempotently from `_QOE_ADJUSTMENT_RULES` + `_QOE_CATEGORY_MAP` at startup (ON CONFLICT DO UPDATE so rule changes propagate). New `services/qoe_flags.py` module exposes `FlagEvidence` Pydantic model + `upsert_flag` (atomic dual-write to edge AND array in one txn), `fetch_flags_for_txn`, `fetch_flagged_txns_in_window` (edge-first with array fallback). Switched `categorization.py` and every seed-script call site (`writers.py`, `archetypes/aldergrove`, `archetypes/brightloom`, `portfolio_extras.py`, `overlays.py`) to route through `upsert_flag` with rich evidence dicts (z_score, dup_count, vendor_mean, etc.). Swapped `qoe_readiness_scorecard` and `qoe_adjustments_report` to read from edges via `fetch_flagged_txns_in_window`; gated confidence-weighted impact aggregation behind `QOE_CONFIDENCE_WEIGHTED` env flag (default off - re-baseline after enable). Frontend: extended `lib/portfolio.ts` `QoeCategory.items[]` and `QoeAdjustmentBucket.examples[]` with optional `fact`/`evidence`/`confidence`; rendered an italic evidence chip below each item in `QoeAdjustmentsCard` and the readiness drill panel showing the `fact` verbatim + a `conf X%` badge when confidence < 1.0. Added one-line capability hint to the Advisory specialist prompt instructing the agent to cite `evidence.fact` verbatim. New `services/qoe_flags_backfill.py` CLI synthesises FLAGGED_AS edges from existing array data with `--tenant-id`, `--demo-tenants`, `--all-tenants`, `--batch-size` flags; idempotent via `ON CONFLICT DO NOTHING` on edges and a deduplicating `unnest(... || ARRAY[$3])` array update.
- **Files changed**: `sorraia/apps/api/sorraia/services/db.py` (new `qoe_flags` DDL + `_seed_qoe_flags` startup hook), `sorraia/apps/api/sorraia/services/qoe_flags.py` (new, ~410 lines), `sorraia/apps/api/sorraia/services/qoe_flags_backfill.py` (new, ~190 lines), `sorraia/apps/api/sorraia/services/categorization.py` (UPDATE block at line 167 replaced with `upsert_flag` loop carrying evidence), `sorraia/apps/api/sorraia/services/portfolio_intelligence.py` (`qoe_adjustments_report` and `qoe_readiness_scorecard` now read via `fetch_flagged_txns_in_window`, optional confidence weighting), `sorraia/apps/api/sorraia/scripts/seed_portfolio/{writers.py,overlays.py,portfolio_extras.py,archetypes/aldergrove/seed.py,archetypes/brightloom/seed.py}` (every direct `anomaly_flags` write routed through `upsert_flag`), `sorraia/apps/api/sorraia/agents/specialists.py` (QoE-provenance bullet added to Advisory prompt), `sorraia/apps/api/tests/conftest.py` (added `edges` to `_PE_CLEANUP_TABLES`), `sorraia/apps/api/tests/services/test_qoe_flags.py` (new, 6 tests), `sorraia/apps/web/lib/portfolio.ts` (+3 optional fields on items & examples), `sorraia/apps/web/components/chat/sorraia-cards.tsx` (evidence chip in `QoeAdjustmentsCard` + readiness drill table).
- **Verified**: All 136 tests pass (130 pre-existing + 6 new QoE-flag tests); zero linter errors across all touched files. Backfill spot-checked: 8 tenants reached perfect parity (166 edges = 166 array flag-pairs); re-running the CLI is idempotent (same edge counts). End-to-end smoke against `aldergrove`: `qoe_adjustments_report` returns reported_ebitda=$2.36M, adjusted=$2.56M, 6 buckets, top bucket "recurring_addback" with `fact='Owner / one-time charge of $8,903.22'` populated; `qoe_readiness_scorecard` returns score=81 status=red. `QOE_CONFIDENCE_WEIGHTED=true` produces identical output to default (all current edges at confidence=1.0 - no regression; weighting infra ready for sub-1.0 LLM-detected flags). Routing eval (`python -m sorraia.eval.runner`) shows the same 0.322 overall_avg as before - pre-existing baseline, not a regression. QoE-related queries route correctly to advisory at conf 0.55 - 0.70.
- **Issues**: (1) `_seed_qoe_flags` initially used `ON CONFLICT DO NOTHING` which left stale `category='uncategorized'` rows for any flag whose definition existed before the canonical rules grew. Fixed to `ON CONFLICT (flag_name) DO UPDATE SET category, direction, rationale = EXCLUDED.*` so rule-table changes propagate on every startup. (2) Backfill CLI initially queried `transactions.vendor` and `transactions.amount` - wrong; columns are `vendor_qbo_id` and `total_amount`. Fixed by JOINing `vendors` (column is `display_name`, not `name`) and selecting `t.total_amount AS amount`. (3) Plan called for an FK from `edges.target_id` to `qoe_flags.flag_name` for FLAGGED_AS edges, but `edges` is polymorphic so a SQL FK is impractical; the invariant is enforced in `qoe_flags.upsert_flag` via `_ensure_flag_row` (auto-creates an `uncategorized` row for unknown flag names) and documented at the top of the module. (4) Flag-name drift between `categorization.py` ("amount_outlier", "potential_duplicate") and `_QOE_ADJUSTMENT_RULES` ("outlier", "possible_duplicate") was tolerated rather than reconciled - `_ensure_flag_row` handles unknown names gracefully and reconciling required touching every reader; deferred. (5) Existing `[:25]` cap on `qoe_readiness_scorecard.categories[].items` is preserved (display-only); `item_count` reflects the true count. (6) `quinn` had zero flagged transactions in the local DB (csv-driven seed flow doesn't run categorization) - backfilled the 7 demo cohort + 2 add-on tenants instead. Backfill marks evidence with `"backfilled": true` so callers can distinguish historical edges from live dual-write evidence.
